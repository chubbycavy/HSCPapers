{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core app only (all file work happens in Rust).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
